import { StyleSheet } from 'react-native';

export const stylesInputPhone = StyleSheet.create({
    mask: {
        position: 'absolute',
        left: 8,
        bottom: 8,
        zIndex: 2,
        elevation: 2,
        width: '100%'
    }
});
